{- Конфигурация веб-сервера -}
PORT := 8080;
SSL_PORT := [+ PORT 443];
TIMEOUT := 30;

webserver := struct{
    host = 'localhost',
    ports = (list PORT SSL_PORT),
    timeout = TIMEOUT,
    workers = [* TIMEOUT 2],
    charset = (chr 85)
};
